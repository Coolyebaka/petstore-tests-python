[tool.black]
line-length = 100
target-version = ["py310"]

[tool.isort]
profile = "black"
line_length = 100
combine_as_imports = true

[tool.flake8]
max-line-length = 100
exclude = [".venv", ".git", "__pycache__"]
